How it works:

In the maps folder, there are map images — they make up the overall map pool. The program selects 3 maps from this pool for each round, excluding the maps that were chosen in previous rounds.

To edit the map pool, simply keep the images of the maps you like in the maps folder. Inside the maps folder, there is also an all maps folder that contains images of all available maps.**
